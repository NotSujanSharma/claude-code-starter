<!-- UNCONFIGURED -->

# Stack

**This project has not been bootstrapped yet.**

Run `/bootstrap`. It will inspect whatever is already here, ask you about anything it
can't determine, then replace this file with the real thing and wire up
`scripts/verify.sh`.

Until that happens, agents working in this repo are guessing about your language,
your commands, and your conventions — which is the exact failure mode this
repository exists to prevent.

Nothing below this line is real. `/bootstrap` writes it.

---

- What this project is
- Language & runtime
- Commands (install · dev · test · single test · typecheck · lint · build)
- Layout
- Conventions an agent would otherwise get wrong
- Landmines
- Never touch
