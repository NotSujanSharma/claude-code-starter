# Contract — <name>

Frozen at Phase 2. Changing this mid-flight invalidates every task built against
it — if it must change, stop, amend here, and tell every in-flight agent.

## Owns
Which module/files are responsible for this.

## Types
```
<type / schema / interface definitions>
```

## Behaviour
- Inputs, outputs, and the invariants callers may rely on.

## Errors
| Condition | Result |
|---|---|
| | |

## Explicitly not guaranteed
- Things callers must not assume (ordering, idempotency, thread-safety, ...).
